---
phase: 12-data-layer-hybrid-builder-removal
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/layout/hybrid_builder.py
  - src/layout/system_view.py
  - src/layout/shell.py
  - src/layout/scorecard.py
  - src/layout/equipment_grid.py
  - app.py
  - src/data/processing.py
autonomous: true
requirements: [DATA-02, DATA-03, CONTENT-03]

must_haves:
  truths:
    - "hybrid_builder.py is deleted from the codebase"
    - "No import of hybrid_builder exists in any file"
    - "No dcc.Store(id='store-hybrid-slots') exists in the DOM"
    - "Hybrid page shows a static equipment table identical in style to mechanical/electrical"
    - "Scorecard renders all three system columns immediately on page load without gating"
    - "No gate overlay, slot dropdowns, or slot counter appear anywhere"
  artifacts:
    - path: "src/layout/system_view.py"
      provides: "Clean system view without hybrid builder imports or gate overlay"
    - path: "src/layout/shell.py"
      provides: "Shell without store-hybrid-slots or SLOT_STAGES import"
    - path: "src/layout/scorecard.py"
      provides: "Scorecard always rendering 3 columns from BOM data"
    - path: "app.py"
      provides: "Entry point without hybrid_builder import"
  key_links:
    - from: "src/layout/system_view.py"
      to: "src/layout/equipment_grid.py"
      via: "make_equipment_section(data['hybrid'], 'hybrid', data)"
      pattern: "make_equipment_section.*hybrid"
    - from: "src/layout/scorecard.py"
      to: "src/data/processing.py"
      via: "compute_scorecard_metrics with all three DataFrames"
      pattern: "compute_scorecard_metrics.*mechanical.*electrical.*hybrid"
---

<objective>
Delete the hybrid builder entirely, remove all its references from the codebase, and wire the hybrid system as a static equipment table with an ungated 3-column scorecard. The hybrid page must look identical to mechanical and electrical pages.

Purpose: The hybrid builder (slot dropdowns, gate overlay, slot counter) is being replaced with a fixed preset from data.xlsx. The hybrid system is now a pre-defined configuration, not user-assembled.

Output: hybrid_builder.py deleted; system_view.py, shell.py, scorecard.py, equipment_grid.py, app.py, and processing.py cleaned of all builder references; hybrid displayed as static equipment table; scorecard shows 3 columns immediately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-data-layer-hybrid-builder-removal/12-CONTEXT.md
@.planning/phases/12-data-layer-hybrid-builder-removal/12-UI-SPEC.md
@.planning/phases/12-data-layer-hybrid-builder-removal/12-01-SUMMARY.md

@src/layout/hybrid_builder.py
@src/layout/system_view.py
@src/layout/shell.py
@src/layout/scorecard.py
@src/layout/equipment_grid.py
@src/data/processing.py
@app.py

<interfaces>
<!-- Key functions the executor needs from Plan 01 output -->

From src/data/loader.py (after Plan 01):
- load_data() returns dict with key "hybrid" (pd.DataFrame, same EQUIPMENT_COLUMNS as mechanical/electrical)
- load_data() returns dict with key "energy" (nested dict by system)
- No "miscellaneous" key exists anymore

From src/config.py (after Plan 01):
- PROCESS_STAGES has key "hybrid" (not "miscellaneous")

From src/layout/equipment_grid.py:
- make_equipment_section(df: pd.DataFrame, system: str, all_data: dict) -> html.Div

From src/data/processing.py:
- compute_scorecard_metrics(mechanical_df, electrical_df, hybrid_df=None)
- compute_hybrid_df(slots, data) — TO BE REMOVED
- generate_comparison_text(hybrid_metrics, mech_metrics, elec_metrics)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete hybrid_builder.py and remove all imports/references</name>
  <files>src/layout/hybrid_builder.py, src/layout/system_view.py, src/layout/shell.py, app.py</files>
  <read_first>
    - src/layout/hybrid_builder.py (understand all exports: make_hybrid_builder, set_data, SLOT_STAGES, and all callback IDs)
    - src/layout/system_view.py (lines 19, 107-109, 137-139, 147-189, 234-239 — hybrid builder import, builder section, equipment container, gate overlay)
    - src/layout/shell.py (lines 21, 92-96 — SLOT_STAGES import, store-hybrid-slots Store)
    - app.py (lines 76-77 — hybrid_builder set_data import and call)
  </read_first>
  <action>
1. **Delete hybrid_builder.py** (per D-13): Remove the file entirely from the filesystem.

2. **Clean system_view.py** (per D-14, D-16, D-17):
   - Remove line 19: `from src.layout.hybrid_builder import make_hybrid_builder`
   - Remove the hybrid builder section (lines 107-109): Delete the `hybrid_builder_section = None` block and the `if active_system == "hybrid": hybrid_builder_section = make_hybrid_builder(data)` block entirely.
   - **Change hybrid equipment rendering** (lines 137-139): Replace the callback-driven `html.Div(id="hybrid-equipment-container")` with a direct call to `make_equipment_section`. The hybrid tab should render equipment identically to mechanical/electrical:
     ```python
     if active_system == "hybrid":
         system_df = data.get("hybrid", data.get("mechanical"))
         equipment = make_equipment_section(system_df, "hybrid", data)
     else:
         system_df = data.get(active_system, data.get("mechanical"))
         equipment = make_equipment_section(system_df, active_system, data)
     ```
     This simplifies to a single block for ALL systems (no special hybrid case):
     ```python
     system_df = data.get(active_system, data.get("mechanical"))
     equipment = make_equipment_section(system_df, active_system, data)
     ```
   - **Remove gate overlay** (per D-16, lines 147-191): Remove the entire `if active_system == "hybrid":` block that creates `gate_overlay` and `chart_wrapper`. All systems use the same chart_wrapper:
     ```python
     chart_wrapper = chart_section
     ```
   - **Remove hybrid_builder_section from assembly** (lines 234-239): Remove the `if hybrid_builder_section is not None:` block that appends the builder to `top_level_children`.
   - **Update scorecard initial render** (line 117): Change from 2-system to 3-system:
     ```python
     initial_scorecard = make_scorecard_table(data["mechanical"], data["electrical"], data.get("hybrid"))
     ```
   - **Update docstring**: Remove all references to hybrid builder, gate overlay, and slot-fill requirements.

3. **Clean shell.py** (per D-14, D-15):
   - Remove line 21: `from src.layout.hybrid_builder import SLOT_STAGES`
   - Remove the `dcc.Store(id="store-hybrid-slots", ...)` block (lines 92-96) from `create_layout()`.
   - Remove the comment about hybrid slot store.

4. **Clean app.py** (per D-14):
   - Remove lines 76-77:
     ```python
     from src.layout.hybrid_builder import set_data as set_hybrid_builder_data
     set_hybrid_builder_data(DATA)
     ```
  </action>
  <verify>
    <automated>cd "C:\Users\kevin\Downloads\Desalination Project Code" && python -c "
import os, sys
# Check hybrid_builder.py is deleted
assert not os.path.exists('src/layout/hybrid_builder.py'), 'hybrid_builder.py still exists'

# Check no imports of hybrid_builder remain
for f in ['app.py', 'src/layout/system_view.py', 'src/layout/shell.py']:
    with open(f) as fh:
        content = fh.read()
        assert 'hybrid_builder' not in content, f'{f} still references hybrid_builder'
        assert 'store-hybrid-slots' not in content, f'{f} still references store-hybrid-slots'

# Check shell.py has no SLOT_STAGES import
with open('src/layout/shell.py') as fh:
    assert 'SLOT_STAGES' not in fh.read(), 'shell.py still imports SLOT_STAGES'

# Check system_view.py has no gate overlay
with open('src/layout/system_view.py') as fh:
    sv = fh.read()
    assert 'hybrid-gate-overlay' not in sv, 'gate overlay still in system_view.py'
    assert 'hybrid-equipment-container' not in sv, 'hybrid-equipment-container still in system_view.py'
    assert 'make_hybrid_builder' not in sv, 'make_hybrid_builder still imported'

print('ALL DELETION CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - src/layout/hybrid_builder.py does NOT exist on disk
    - app.py does NOT contain "hybrid_builder"
    - src/layout/system_view.py does NOT contain "hybrid_builder", "hybrid-gate-overlay", "hybrid-equipment-container", or "make_hybrid_builder"
    - src/layout/shell.py does NOT contain "SLOT_STAGES" or "store-hybrid-slots"
    - system_view.py renders hybrid equipment via make_equipment_section(system_df, "hybrid", data) — same pattern as mechanical/electrical
    - system_view.py initial scorecard call includes hybrid_df as third argument
  </acceptance_criteria>
  <done>hybrid_builder.py deleted; all imports and references removed from system_view.py, shell.py, and app.py; hybrid tab renders static equipment table; no gate overlay or slot UI in codebase</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite scorecard and processing to use hybrid BOM directly (no slots)</name>
  <files>src/layout/scorecard.py, src/data/processing.py, src/layout/equipment_grid.py</files>
  <read_first>
    - src/layout/scorecard.py (update_scorecard callback on line 318 using store-hybrid-slots; update_gate_overlay callback on line 379; update_hybrid_equipment callback on line 423)
    - src/data/processing.py (compute_hybrid_df function on line 277 — slot-based, to be removed; compute_chart_data on line 551 — hybrid_df parameter)
    - src/layout/equipment_grid.py (make_equipment_section hybrid branch on line 376 — checks hybrid_selected key)
  </read_first>
  <action>
1. **Rewrite scorecard.py callbacks** (per D-12):
   - **Remove `update_scorecard` callback** (lines 318-376): This callback listened to `store-hybrid-slots` to gate the scorecard. Replace it with a simpler callback OR remove it entirely. Since the scorecard is now always 3-column and rendered initially by system_view.py with all three DataFrames, the callback may not be needed at all. However, the scorecard needs to update when no dynamic inputs change — so the simplest approach is:
     - Remove the `update_scorecard` callback entirely. The initial render in system_view.py already calls `make_scorecard_table(data["mechanical"], data["electrical"], data.get("hybrid"))` which produces the 3-column table.
     - The `scorecard-container` div and `comparison-text` div still exist in system_view.py, but their content is set at render time and does not need dynamic updates (no slots to trigger changes).
   - **Remove `update_gate_overlay` callback** (lines 379-420): Delete entirely — gate overlay div no longer exists.
   - **Remove `update_hybrid_equipment` callback** (lines 423-457): Delete entirely — hybrid equipment is now rendered statically in system_view.py.
   - **Remove imports no longer needed**: `compute_hybrid_df` is no longer used in scorecard.py. Remove it from the import line. Keep `compute_scorecard_metrics`, `generate_comparison_text`, `rag_color`, `fmt_cost`, `fmt_sig2`.
   - **Remove `make_equipment_section` import** (line 36): No longer needed in scorecard.py since hybrid equipment rendering moved to system_view.py.
   - **Generate comparison text at initial render**: Since comparison text was previously callback-driven, we need to ensure it still appears. Update `make_scorecard_table` to accept an optional `comparison_text` parameter, OR have system_view.py generate comparison text separately and place it in the `comparison-text` div. The simplest approach: have system_view.py generate comparison text:
     ```python
     # In system_view.py, after the scorecard:
     from src.data.processing import compute_scorecard_metrics, generate_comparison_text
     metrics = compute_scorecard_metrics(data["mechanical"], data["electrical"], data.get("hybrid"))
     if "hybrid" in metrics:
         comparison_str = generate_comparison_text(metrics["hybrid"], metrics["mechanical"], metrics["electrical"])
         comparison_content = html.P(comparison_str, className="text-muted small mt-2", style={"fontStyle": "italic"})
     else:
         comparison_content = None
     comparison_text_div = html.Div(comparison_content, id="comparison-text")
     ```

2. **Clean processing.py**:
   - **Remove `compute_hybrid_df` function** (lines 277-320): This slot-based function is no longer needed. The hybrid BOM comes directly from `data["hybrid"]`.
   - **Update `compute_chart_data` signature**: Remove the `hybrid_df` parameter. Instead, read hybrid data from `data["hybrid"]` directly:
     ```python
     def compute_chart_data(data, battery_fraction=0.5, years=50, tds_ppm=950, depth_m=950):
     ```
     Inside the function:
     - Replace `if hybrid_df is not None:` checks with direct reads from `data["hybrid"]`
     - Hybrid cost over time: `hybrid_cumulative = compute_cost_over_time(data["hybrid"], years)`
     - Hybrid land area: `hybrid_land = float(pd.to_numeric(data["hybrid"]["land_area_m2"], errors="coerce").sum())`
     - Hybrid energy breakdown: `hybrid_energy = _energy_by_stage(data["hybrid"], "hybrid")`
     - Remove the `hybrid_df` parameter from docstring
   - **Update `generate_comparison_text`**: No changes needed — it takes metric dicts, not DataFrames.

3. **Clean equipment_grid.py** (per D-11):
   - **Simplify hybrid branch** in `make_equipment_section` (lines 376-403): Remove the special `hybrid_selected` key lookup. When system is "hybrid", treat it exactly like mechanical/electrical — group by process stage using `get_equipment_stage(name, "hybrid")` and render accordion items. The current code has a special branch that checks `all_data.get("hybrid_selected")` — remove this entirely. The hybrid case should fall through to the standard stage-grouped rendering (lines 405-450).
   - Specifically, remove lines 376-403 (the entire `if system == "hybrid":` block) so that ALL systems use the same code path: group by stage, build accordion items.
   - The cross-system comparison in `_make_cross_system_comparison` (line 169) currently only checks mechanical and electrical. Update it to also check hybrid:
     ```python
     other_systems = [s for s in ("mechanical", "electrical", "hybrid") if s != system]
     ```
  </action>
  <verify>
    <automated>cd "C:\Users\kevin\Downloads\Desalination Project Code" && python -c "
# Check scorecard.py has no slot references
with open('src/layout/scorecard.py') as f:
    sc = f.read()
    assert 'store-hybrid-slots' not in sc, 'scorecard still references store-hybrid-slots'
    assert 'hybrid-gate-overlay' not in sc, 'scorecard still references hybrid-gate-overlay'
    assert 'hybrid-equipment-container' not in sc, 'scorecard still references hybrid-equipment-container'
    assert 'compute_hybrid_df' not in sc, 'scorecard still imports compute_hybrid_df'

# Check processing.py has no compute_hybrid_df
with open('src/data/processing.py') as f:
    proc = f.read()
    assert 'def compute_hybrid_df' not in proc, 'compute_hybrid_df still defined'

# Check equipment_grid.py has no hybrid_selected
with open('src/layout/equipment_grid.py') as f:
    eg = f.read()
    assert 'hybrid_selected' not in eg, 'equipment_grid still references hybrid_selected'

print('ALL CLEANUP CHECKS PASSED')
" && python -c "
# Smoke test: import app without errors
import sys
sys.argv = ['app']
import app as application
print('App import successful')
print('Data keys:', list(application.DATA.keys()))
"
    </automated>
  </verify>
  <acceptance_criteria>
    - scorecard.py does NOT contain "store-hybrid-slots", "hybrid-gate-overlay", "hybrid-equipment-container", or "compute_hybrid_df"
    - scorecard.py does NOT have update_scorecard, update_gate_overlay, or update_hybrid_equipment callbacks
    - processing.py does NOT contain "def compute_hybrid_df"
    - processing.py compute_chart_data does NOT have hybrid_df parameter
    - equipment_grid.py does NOT contain "hybrid_selected"
    - equipment_grid.py _make_cross_system_comparison checks mechanical, electrical, AND hybrid
    - python -c "import sys; sys.argv=['app']; import app" exits 0
  </acceptance_criteria>
  <done>Scorecard renders 3 columns from BOM data without gating; processing layer reads hybrid directly from data dict; equipment grid treats hybrid identically to other systems; app imports cleanly</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -c "import sys; sys.argv=['app']; import app; print('OK')"` — app imports without errors
2. `find src/ -name "hybrid_builder.py"` returns nothing
3. `grep -r "store-hybrid-slots" src/ app.py` returns nothing
4. `grep -r "hybrid-gate-overlay" src/ app.py` returns nothing
5. `grep -r "compute_hybrid_df" src/` returns nothing
6. `grep -r "hybrid_selected" src/` returns nothing
</verification>

<success_criteria>
- hybrid_builder.py is deleted
- Zero references to hybrid builder, slot store, gate overlay, or slot-based hybrid computation remain
- Hybrid tab shows static equipment table grouped by process stage
- Scorecard shows 3 columns (Mechanical, Electrical, Hybrid) on initial page load
- App starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-layer-hybrid-builder-removal/12-02-SUMMARY.md`
</output>
