---
phase: 17-ui-polish-chart-legend
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - .planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md
  - .planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md
  - .planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md
autonomous: false
requirements: [POLISH-01, POLISH-03, POLISH-04]
must_haves:
  truths:
    - "User clicking sidebar Overview link from any system tab reliably returns to the landing page and sidebar nav-overview active state updates correctly (both directions)"
    - "User triggering a data-load error (by renaming data.xlsx) sees the existing error_page.py render with a readable Unable to Load Dashboard heading and the error message"
    - "User printing the scorecard page (Ctrl+P) produces output that includes the RAG scorecard table, cost-over-time chart with in-chart legend visible, power breakdown chart, and the system badge"
    - "User printing the scorecard page does NOT see sidebar, navbar, system tabs, back-to-overview link, chart control panel sliders, or badge pill row in the printed output"
  artifacts:
    - path: ".planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md"
      provides: "Written record of POLISH-01 end-to-end nav verification (D-09, D-10)"
      contains: "nav-overview"
    - path: ".planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md"
      provides: "Written record of POLISH-03 error page verification (D-04, D-05)"
      contains: "create_error_page"
    - path: ".planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md"
      provides: "Written record of POLISH-04 print output verification (D-06, D-07, D-08)"
      contains: "scorecard"
  key_links:
    - from: "nav-overview click"
      to: "active-system store → render_content"
      via: "nav_overview_click callback at shell.py:214"
      pattern: "nav_overview_click"
    - from: "create_error_page"
      to: "app startup data load"
      via: "app.py error boundary (wherever load_data is called)"
      pattern: "create_error_page"
---

<objective>
Verify the three Phase 17 requirements that are CONTEXT-declared as verify-only (D-04, D-05, D-08, D-10): sidebar Overview nav round-trip, data-load error page rendering, and print/export output fidelity. These require running the app and having a human confirm visual/functional correctness, so this plan is NOT autonomous.

Purpose: Close POLISH-01, POLISH-03, and POLISH-04 via end-to-end verification rather than code changes. Plan 01 already applied the code/CSS changes that Plan 02 depends on (badge no-print class, in-chart legend visible in print).

Output:
- Three verification records committed to the phase directory documenting what was tested and the result
- Human checkpoint gates confirming each requirement is satisfied
- Bug-fix task slot available if any verification fails (executor creates a follow-up task inline)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-ui-polish-chart-legend/17-CONTEXT.md
@.planning/phases/17-ui-polish-chart-legend/17-01-PLAN.md

@src/layout/shell.py
@src/layout/error_page.py
@src/layout/system_view.py
@src/layout/scorecard.py
@assets/custom.css
@app.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated smoke check — start app and probe routes/callbacks programmatically</name>
  <files>.planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md</files>
  <read_first>
    - src/layout/shell.py (lines 180-240 — nav_overview_click, update_nav_overview_active callbacks)
    - app.py (root — confirm app entry point and data load site)
    - .planning/phases/17-ui-polish-chart-legend/17-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Perform automated pre-checks before the human verification checkpoint. This task does NOT require human interaction — it prepares evidence for the checkpoint task that follows.

    (1) **Verify nav callbacks are wired correctly** by grep-checking structure in src/layout/shell.py:
    - Confirm `nav_overview_click` callback exists with `Output("active-system", "data", allow_duplicate=True)` and `Input("nav-overview", "n_clicks")`
    - Confirm `update_nav_overview_active` callback exists with `Output("nav-overview", "active")` and `Input("active-system", "data")`
    - Confirm `nav-overview` id exists in the sidebar layout (grep for `id="nav-overview"` or `id='nav-overview'`)

    (2) **Start the app in background and hit the root URL** to confirm it boots:
    ```bash
    python app.py &  # or whatever start command is standard
    APP_PID=$!
    sleep 3
    curl -sf http://127.0.0.1:8050/ > /tmp/nav-root.html && echo "APP_BOOT_OK"
    grep -o 'id="nav-overview"' /tmp/nav-root.html && echo "NAV_OVERVIEW_RENDERED"
    kill $APP_PID
    ```
    If the standard run command differs (check README or app.py `if __name__`), use the correct one. Actual port may be 8050 (Dash default) — confirm from app.py.

    (3) **Write verification record** at `.planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md` with the following exact structure:
    ```markdown
    # POLISH-01 Nav Verification (D-09, D-10)

    ## Callback wiring (grep evidence)
    - nav_overview_click defined at shell.py:<LINE>
    - update_nav_overview_active defined at shell.py:<LINE>
    - id="nav-overview" rendered in HTML: <YES|NO>

    ## Boot smoke test
    - Command: <exact command used>
    - HTTP 200 on /: <YES|NO>
    - nav-overview element present in initial HTML: <YES|NO>

    ## Human UAT — pending
    See Task 2 checkpoint for click-through verification.
    ```
    Fill in actual line numbers and YES/NO values.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md && grep -q "nav_overview_click defined at shell.py" .planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md` exists
    - File contains `nav_overview_click defined at shell.py:` followed by a line number
    - File contains `update_nav_overview_active defined at shell.py:` followed by a line number
    - File contains `HTTP 200 on /:` followed by YES or NO
    - File contains `nav-overview element present in initial HTML:` with YES or NO
    - If app boot or curl failed, the file honestly records NO (do not fabricate success)
  </acceptance_criteria>
  <done>Nav verification record written with factual grep+HTTP evidence; file ready for human checkpoint follow-up in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — sidebar nav, error page, and print output (POLISH-01, POLISH-03, POLISH-04)</name>
  <read_first>
    - .planning/phases/17-ui-polish-chart-legend/17-NAV-VERIFICATION.md (created in Task 1)
    - .planning/phases/17-ui-polish-chart-legend/17-CONTEXT.md (D-05, D-06, D-07, D-08, D-10)
    - src/layout/error_page.py (the component under test)
    - assets/custom.css (confirm @media print rules lines 131-174)
  </read_first>
  <files>.planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md, .planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md</files>
  <action>Run the three UAT scenarios in <how-to-verify> (A: sidebar nav round-trip, B: data-load error page, C: print/export output). After each scenario, write a PASS/FAIL record for Scenarios B and C to the phase directory as 17-ERROR-VERIFICATION.md and 17-PRINT-VERIFICATION.md respectively. Each file must list every expected item from the scenario's checklist with an explicit PASS or FAIL marker and any observed notes. Do NOT type "approved" until all three scenarios pass; if any item fails, append a follow-up bug-fix task inline with the specific code change needed.</action>
  <what-built>
    Plan 01 added the Plotly in-chart legend, marked the badge pill row `no-print legend-badge-row`, added a `.legend-badge-row { display:none !important }` rule in @media print, and normalized heading visual hierarchy via .section-heading / .subsection-heading CSS classes. Plan 02 Task 1 auto-confirmed the nav callbacks and boot. This checkpoint asks the human to run three quick UAT scenarios and confirm.
  </what-built>
  <how-to-verify>
    **Start the app locally:** `python app.py` (or equivalent) and open http://127.0.0.1:8050/ in a browser.

    ### Scenario A — Sidebar Overview nav round-trip (POLISH-01, D-10)
    1. Land on Overview page. Confirm sidebar "Overview" link appears active (highlighted).
    2. Click the "Explore" button on any system card (e.g., Mechanical) → navigates to system tab.
    3. Confirm sidebar "Overview" link is NO LONGER active (not highlighted).
    4. Click the sidebar "Overview" link.
    5. **Expected:** Returns to the landing page. Sidebar "Overview" link becomes active again. No console errors.
    6. Repeat steps 2-5 for Electrical and Hybrid tabs.

    ### Scenario B — Data-load error page (POLISH-03, D-05)
    1. Stop the app (Ctrl+C).
    2. Temporarily rename `data.xlsx` → `data.xlsx.bak` (or move to a backup path).
    3. Start the app: `python app.py`.
    4. Open http://127.0.0.1:8050/.
    5. **Expected:** You see the `create_error_page` output with "Unable to Load Dashboard" heading in red, a paragraph explaining the data file could not be read, a red alert box with the actual error message, and optionally a collapsed "Details (for technical users)" accordion.
    6. Confirm the page is readable and the error message is identifiable.
    7. Stop the app. Rename `data.xlsx.bak` → `data.xlsx`. Restart the app and confirm it works normally again.

    ### Scenario C — Print/export output (POLISH-04, D-06, D-07, D-08)
    1. App running, navigate to any system tab (e.g., Mechanical) with charts visible.
    2. Ensure the Cost Over Time chart shows the new Plotly in-chart legend (top-right, horizontal).
    3. Open print preview: Ctrl+P (Chrome recommended — matches dbc GitHub #269 fix context).
    4. **Expected in print preview:**
       - ✅ RAG scorecard table visible
       - ✅ Cost Over Time chart visible WITH the in-chart Plotly legend still showing Mechanical / Electrical / Hybrid labels
       - ✅ Power Breakdown chart visible
       - ✅ System badge (colored system identifier) visible
       - ✅ System accent top border (4px colored line on page-content) visible
       - ❌ Sidebar NOT visible
       - ❌ Navbar NOT visible
       - ❌ System tabs NOT visible
       - ❌ Back-to-overview link NOT visible
       - ❌ Chart control panel sliders (time, battery, TDS, depth) NOT visible
       - ❌ Badge pill legend row ("Systems: Mechanical/Electrical/Hybrid") NOT visible (NEW in Phase 17)
       - ❌ Export/Print button NOT visible
       - ✅ Layout dropdown (portrait/landscape) available in print dialog
    5. Confirm the Page Layout dropdown is present (indicates @page rule is working — dbc #269 fix).

    After completing all three scenarios, write the results to the phase dir:
    ```
    .planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md
    .planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md
    ```
    Each file should record PASS/FAIL per expected item above, plus any issues found.

    If Scenario C reveals the badge row IS still visible in print, that means Plan 01 Task 1 CSS edit did not land — halt and re-open Plan 01.
    If Scenario A reveals the nav link does nothing, investigate shell.py:214-233 callback firing and report details.
    If Scenario B reveals a broken/missing error page, investigate whether app.py actually invokes `create_error_page` on load failure (may need a follow-up fix).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms Scenario A passes (nav round-trip works both directions)
    - Human confirms Scenario B passes (error page renders with readable message)
    - Human confirms Scenario C passes (print output matches expected whitelist AND the NEW "badge pill row hidden" item is true)
    - `.planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md` exists and records PASS or FAIL with notes
    - `.planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md` exists and records PASS or FAIL per expected item
    - If any scenario FAILS, a follow-up bug-fix task is appended inline with specific code changes identified (do not mark checkpoint approved until fix is applied)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md && test -f .planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md && grep -qE "PASS|FAIL" .planning/phases/17-ui-polish-chart-legend/17-ERROR-VERIFICATION.md && grep -qE "PASS|FAIL" .planning/phases/17-ui-polish-chart-legend/17-PRINT-VERIFICATION.md</automated>
  </verify>
  <done>Human has run Scenarios A/B/C, recorded PASS/FAIL in 17-ERROR-VERIFICATION.md and 17-PRINT-VERIFICATION.md, and typed "approved" (or a follow-up fix has been applied and the scenarios re-run to PASS).</done>
  <resume-signal>Type "approved" after all three scenarios pass and verification MD files are written, or describe specific issues found for follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan performs verification only and creates markdown files in the planning directory. No code execution surface is added, no new inputs are accepted. Trust boundaries: none beyond those already present in the app (none of note for a local Dash tool).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Information Disclosure | Error page showing traceback to user (D-05) | accept | Error page exposes internal file paths / tracebacks in the collapsed "Details" accordion. Accepted because this is a local-use academic dashboard — the user running the app owns the filesystem already. Not a disclosure risk. |
| T-17-06 | Tampering / Spoofing / Repudiation / DoS / Elevation | — | N/A | No identity, no persistence, no network-exposed mutation, no privilege boundaries in this plan's scope (markdown file creation only). |
</threat_model>

<verification>
1. `ls .planning/phases/17-ui-polish-chart-legend/17-{NAV,ERROR,PRINT}-VERIFICATION.md` — all three files exist
2. `grep -l "PASS\|FAIL" .planning/phases/17-ui-polish-chart-legend/17-*-VERIFICATION.md` — each records an explicit result
3. Human has typed "approved" on the checkpoint (or a follow-up fix has been applied and re-verified)
</verification>

<success_criteria>
- POLISH-01: Sidebar Overview nav link verified end-to-end (D-09, D-10)
- POLISH-03: Data-load error page verified to render correctly on data.xlsx failure (D-04, D-05)
- POLISH-04: Print output verified — scorecard + both charts + system badge visible; sidebar/navbar/tabs/controls/badge-row hidden (D-06, D-07, D-08)
- Three verification markdown records committed to phase directory
- Zero regressions introduced (this plan only reads code and writes docs)
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-polish-chart-legend/17-02-SUMMARY.md`
</output>
