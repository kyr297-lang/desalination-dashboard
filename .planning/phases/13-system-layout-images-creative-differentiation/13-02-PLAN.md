---
phase: 13-system-layout-images-creative-differentiation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/layout/equipment_grid.py
autonomous: false
requirements:
  - VISUAL-04

must_haves:
  truths:
    - "Mechanical equipment stage headings have a left border accent bar in steel blue (#5B8DB8)"
    - "Electrical equipment stage headings have a bottom border underline in terra cotta (#D4854A)"
    - "Hybrid equipment stage headings have no extra accent (default styling)"
    - "Mechanical and electrical system pages are visually distinguishable beyond color border-top -- different card accents AND different stage heading treatments"
  artifacts:
    - path: "src/layout/equipment_grid.py"
      provides: "System-specific CSS class on stage H5 headings"
      contains: "stage-heading-mechanical"
  key_links:
    - from: "src/layout/equipment_grid.py"
      to: "assets/custom.css"
      via: "CSS class names on H5 elements"
      pattern: "stage-heading-mechanical"
---

<objective>
Apply system-specific CSS classes to equipment stage headings in equipment_grid.py and visually verify the complete Phase 13 output (diagrams + differentiation) across all three system pages.

Purpose: Completes VISUAL-04 by extending the creative differentiation beyond the diagram card to the equipment section stage headings. Mechanical gets a left-bar accent (pipeline of stages), electrical gets a bottom-line accent (linear flow). Combined with Plan 01's diagram card styling, this delivers structural differentiation between mechanical and electrical pages.

Output: Updated equipment_grid.py with conditional stage heading classes, human-verified visual output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-system-layout-images-creative-differentiation/13-CONTEXT.md
@.planning/phases/13-system-layout-images-creative-differentiation/13-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan depends on. -->

From assets/custom.css (added by Plan 01):
```css
.stage-heading-mechanical {
    border-left: 3px solid #5B8DB8;
    padding-left: 0.5rem;
}

.stage-heading-electrical {
    border-bottom: 2px solid #D4854A;
    padding-bottom: 0.25rem;
}
```

From src/layout/equipment_grid.py (current state):
```python
def make_equipment_section(df: pd.DataFrame, system: str, all_data: dict) -> html.Div:
    # system parameter is already available -- one of "mechanical", "electrical", "hybrid"
    # Stage H5 is rendered at line 408: html.H5(stage, className="mt-4 mb-2")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add system-specific CSS classes to equipment stage headings</name>
  <files>src/layout/equipment_grid.py</files>
  <read_first>
    - src/layout/equipment_grid.py (current stage heading rendering -- find the html.H5 inside the stage section loop)
    - assets/custom.css (verify .stage-heading-mechanical and .stage-heading-electrical classes exist from Plan 01)
  </read_first>
  <action>
Modify the stage section loop in `make_equipment_section` in `src/layout/equipment_grid.py`. The current code (around line 406-415) builds stage sections like this:

```python
        sections.append(
            html.Div([
                html.H5(stage, className="mt-4 mb-2"),
                dbc.Accordion(
                    accordion_items,
                    always_open=False,
                    active_item=None,
                    className="shadow-sm",
                ),
            ])
        )
```

Replace the `html.H5(stage, className="mt-4 mb-2")` line with conditional class logic that adds the system-specific stage heading class for mechanical and electrical:

```python
        # System-specific stage heading accent (Phase 13 VISUAL-04)
        stage_class = "mt-4 mb-2"
        if system == "mechanical":
            stage_class += " stage-heading-mechanical"
        elif system == "electrical":
            stage_class += " stage-heading-electrical"

        sections.append(
            html.Div([
                html.H5(stage, className=stage_class),
                dbc.Accordion(
                    accordion_items,
                    always_open=False,
                    active_item=None,
                    className="shadow-sm",
                ),
            ])
        )
```

This means:
- Mechanical pages: every stage H5 gets class `mt-4 mb-2 stage-heading-mechanical` (renders with a 3px left border bar in #5B8DB8 and 0.5rem left padding)
- Electrical pages: every stage H5 gets class `mt-4 mb-2 stage-heading-electrical` (renders with a 2px bottom border in #D4854A and 0.25rem bottom padding)
- Hybrid pages: every stage H5 keeps class `mt-4 mb-2` (no extra accent -- neutral)

Do NOT change any other part of the function. The `system` parameter is already available as the second argument to `make_equipment_section`.
  </action>
  <verify>
    <automated>cd "C:/Users/kevin/Downloads/Desalination Project Code" && python -c "from src.layout.equipment_grid import make_equipment_section; print('import OK')" && grep -c "stage-heading-mechanical" src/layout/equipment_grid.py && grep -c "stage-heading-electrical" src/layout/equipment_grid.py && grep "stage_class" src/layout/equipment_grid.py</automated>
  </verify>
  <acceptance_criteria>
    - src/layout/equipment_grid.py contains `stage_class = "mt-4 mb-2"`
    - src/layout/equipment_grid.py contains `if system == "mechanical":`
    - src/layout/equipment_grid.py contains `stage_class += " stage-heading-mechanical"`
    - src/layout/equipment_grid.py contains `elif system == "electrical":`
    - src/layout/equipment_grid.py contains `stage_class += " stage-heading-electrical"`
    - src/layout/equipment_grid.py contains `html.H5(stage, className=stage_class)`
    - The original `html.H5(stage, className="mt-4 mb-2")` hardcoded line is removed (replaced by the conditional)
    - `python -c "from src.layout.equipment_grid import make_equipment_section"` exits 0
  </acceptance_criteria>
  <done>Equipment stage headings render with system-specific CSS classes: mechanical gets left-bar accent, electrical gets bottom-line accent, hybrid stays neutral. Import succeeds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of diagrams and differentiation</name>
  <what-built>
Complete Phase 13 implementation across Plans 01 and 02:
- Three system layout PNG diagrams displayed at the top of each system page (before scorecard)
- Mechanical page: diagram card has 4px left blue border accent + subtle background tint; equipment stage headings have left-bar accent
- Electrical page: diagram card has 2px top terra cotta border accent; equipment stage headings have bottom-line accent
- Hybrid page: diagram card and stage headings are neutral (no extra accent)
- All diagrams visible in browser print preview
  </what-built>
  <how-to-verify>
1. Start the app: `python app.py`
2. Click the **Mechanical** tab:
   - Verify: a full-width PNG diagram appears at the TOP of the page, BEFORE the scorecard
   - Verify: the diagram card has a visible blue left border accent (4px solid #5B8DB8)
   - Verify: equipment stage headings (Water Extraction, Desalination, etc.) have a blue left border bar
3. Click the **Electrical** tab:
   - Verify: a full-width PNG diagram appears at the top, before the scorecard
   - Verify: the diagram card has a visible terra cotta top border accent (different from mechanical's left border)
   - Verify: equipment stage headings have a terra cotta bottom underline (different from mechanical's left bar)
4. Click the **Hybrid** tab:
   - Verify: a full-width PNG diagram appears at the top, before the scorecard
   - Verify: the diagram card has NO extra border accent (just the standard shadow-sm card)
5. Use browser **Ctrl+P** (print preview):
   - Verify: diagrams appear in the print preview (not hidden)
   - Verify: border accents are preserved in print
6. Overall: mechanical and electrical pages are visually distinguishable beyond just the top color border from Phase 9
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "from src.layout.equipment_grid import make_equipment_section; print('OK')"` -- import succeeds
2. `grep "stage-heading-mechanical" src/layout/equipment_grid.py` -- class applied conditionally
3. `grep "stage-heading-electrical" src/layout/equipment_grid.py` -- class applied conditionally
4. `python app.py` runs and all three tabs show diagrams with correct styling
</verification>

<success_criteria>
- Equipment stage headings on mechanical pages have left-bar accent
- Equipment stage headings on electrical pages have bottom-line accent
- Hybrid stage headings are unstyled (neutral)
- Human confirms all three system pages show diagrams prominently
- Human confirms mechanical and electrical are visually distinct beyond color
- Diagrams appear in print preview
</success_criteria>

<output>
After completion, create `.planning/phases/13-system-layout-images-creative-differentiation/13-02-SUMMARY.md`
</output>
